evaluationDependsOn("$parent.path")

description = 'Implementation Commons (for Implementation Library and Koloboke Compile)'

dependencies {
    compile project("$parent.path:api")
}

compileCompatibility(compileTestJava, 1.8)